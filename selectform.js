function handleSelection(type) {
    if (type === 'student') {
        
        // Add logic for navigation or response
        // window.location.href = 'student-page.html'; // Example: redirect to student page
    } else if (type === 'faculty') {
        alert('Faculty selected! Navigating to faculty page...');
        // Add logic for navigation or response
        // window.location.href = 'faculty-page.html'; // Example: redirect to faculty page
    }
}